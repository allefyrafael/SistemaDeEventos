/**
 * Re-export do contrato de modulos / config de evento (fonte: @eventpass/shared).
 */
export {
  FEATURE_MODULES,
  type FeatureModule,
  eventConfigSchema,
  type EventConfig,
} from '@eventpass/shared';
